<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Meetup Info</title>
  </head>
  <body>
    <h1> <%= @meetup.title %> </h1>
    <li>Location: <%= @meetup.location.name %></li>
    <li>Date: <%= @meetup.meetup_date %></li>
    <li>Creator: <%= @creator.username %></li>
    <li>Description: <%= @meetup.description %></li>
    <h3>Who is attending?</h3>
     <% @users.each do |user| %>
     <% name = User.find(user) %>
     <li><%= name.username %>  <img src="<%= name.avatar_url %>" width="100" height="100" alt="User Avatar"></li>
     <% end %> 
  </body>
</html>
